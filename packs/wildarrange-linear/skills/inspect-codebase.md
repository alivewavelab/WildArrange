# inspect-codebase

## 用途

只读检索本地代码库，为计划、调试、复核或仓库治理提供文件、符号、调用关系和命令证据。

## 流程

1. 读取目标路径适用的项目规则。
2. 优先使用 `rg`、glob 和已配置的 AST/LSP 工具。
3. 返回精确路径、相关符号、匹配原因、可运行命令与未知项。
4. 找不到时说明搜索范围和关键词，不用猜测补齐。

## 禁止

- 不编辑文件或运行会改变状态的命令。
- 不把搜索结果直接当作完成证明。
