# WildArrange Claude 宿主入口

本文件只负责让 Claude 系宿主发现项目治理规范，不复制第二份规则。

开始任何工作前必须：

1. 完整读取根 [`AGENTS.md`](./AGENTS.md)，它是项目规范唯一真相源。
2. 进入 `bin/`、`src/`、五区、`test/`、`doc/` 或 `packs/wildarrange-linear/` 修改前，再读取该目录最近的 `AGENTS.md`。
3. 若本文件与 `AGENTS.md` 冲突，以 `AGENTS.md` 为准；不要在这里补充或覆盖项目规则。
