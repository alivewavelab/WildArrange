# 代码注释规范（通用版）

适用于任意编程语言与项目类型（Web、客户端、后端、脚本、游戏引擎等）。  
复制本文件到你的仓库根目录或 `docs/`，按项目约定改「所属模块」命名即可。

> **与语言专用版的关系**  
> 若项目有引擎/框架专用规范（如 Unity C#），可在专用文档中引用本通用版，并只补充框架差异项。

---

## 1. 总则

| 原则 | 说明 |
|------|------|
| 只加注释，不改逻辑 | 注释任务不得改变可执行代码、公开 API 签名或控制流 |
| 中文为主 | 文件头、「运行原理速读」、类型与成员说明使用简体中文（对外开源库可按团队约定用英文） |
| 运行原理 ≠ 操作教程 | 解释系统**如何运转**；不写「安装 IDE、点哪个菜单、克隆仓库」等纯操作步骤 |
| 全覆盖 | 每个源文件有文件头；每个对外/对内重要的类型与成员有说明（含模块内 `private`） |
| 说意图，不复读代码 | 注释回答「为什么 / 在链路里干什么」，避免 `i++ // i 加 1` |

---

## 2. 文件头（必须）

每个源文件**最顶部**（语言要求的 shebang、`package`/`import`/`using` 之前或之后按语言惯例，**统一一种即可**）放固定分隔块。

### 2.1 结构（语言无关）

```
=============================================================================
文件名称：<文件名>
所属模块：<业务模块或目录，如 auth / billing / editor>
作用说明：
  用 2～4 行说明本文件职责边界，读者不打开代码也能知道「管什么、不管什么」。

【运行原理速读】
  （可选）一句比喻，如：「可以把它想成……」

  · <小节 1>？
    何时执行 / 谁调用 / 触发条件。

  · 它具体做了什么？
    ① … ② … ③ … 主流程。

  · 和其他部分的关系？（可选）
    上下游、依赖、被谁调用。

  · 缺了它会怎样？（可选）
    故障表现、降级行为。
=============================================================================
```

### 2.2 各语言写法示例（任选一种，全项目统一）

**C / C++ / C# / Java / Go / Rust / Swift / Kotlin 等（`//`）**

```csharp
// =============================================================================
// 文件名称：undo_service.ts  → 按实际扩展名填写
// 所属模块：editor
// 作用说明：
//   …
//
// 【运行原理速读】
//   …
// =============================================================================
```

**Python / Shell / YAML 内嵌脚本（`#`）**

```python
# =============================================================================
# 文件名称：order_service.py
# 所属模块：order
# 作用说明：
#   …
#
# 【运行原理速读】
#   …
# =============================================================================
```

**JavaScript / TypeScript / CSS（`//` 或块注释）**

```typescript
// =============================================================================
// 文件名称：apiClient.ts
// …
// =============================================================================
```

**HTML / XML（`<!-- -->` 放文件最前）**

```html
<!--
=============================================================================
文件名称：checkout.html
…
=============================================================================
-->
```

**多行块注释语言（C 系 `/* */`）**

```c
/*
 * =============================================================================
 * 文件名称：buffer.c
 * …
 * =============================================================================
 */
```

### 2.3 「运行原理速读」写什么

**要写：**

- 本文件在**端到端链路**中的角色（网关、调度器、缓存、适配器等）
- **触发时机**（HTTP 请求、消息队列、定时任务、用户事件、启动钩子）
- **输入 → 处理 → 输出**（数据形态或副作用）
- 与配置、数据库、外部 API、其他模块的协作关系
- 关键约束（幂等、事务边界、并发、失败重试）

**不要写：**

- 如何安装运行时、IDE 快捷键、仓库克隆步骤
- 仅罗列命令而不解释「执行后系统状态如何变化」（CLI 工具除外：应写命令触发的**业务原理**）

### 2.4 按文件类型选用小节（选 3～4 个即可）

| 类型 | 建议小节 |
|------|----------|
| 入口 / 启动 / 主程序 | 何时启动、初始化什么、失败会怎样 |
| HTTP / RPC 处理器 | 谁调用、请求进来到哪去、错误如何返回 |
| 领域服务 / 业务逻辑 | 触发条件、核心规则、依赖哪些存储或外部服务 |
| 数据访问 / Repository | 读写哪张表/集合、缓存策略、一致性 |
| 前端页面 / 组件 | 用户看到什么、状态从哪来、与后端如何同步 |
| 后台任务 / Worker | 调度周期、消费什么消息、重试与死信 |
| 纯工具 / 算法 | 谁调用、输入输出约定、边界情况 |
| CLI / 构建脚本 | 命令改变什么产物或配置（仍讲原理，非逐步点击教程） |

---

## 3. 类型与成员注释

语法因语言而异，**语义要求一致**：每个重要的类型、常量、字段、方法/函数都有中文说明。

### 3.1 类型（class / struct / interface / type / enum）

| 语言 | 推荐形式 |
|------|----------|
| C#, Java, Kotlin, Swift | `///` 或 `/** */` 文档注释 |
| TypeScript | JSDoc `/** */` |
| Python | 模块/类 docstring |
| Go | 类型上方 `//` 或包注释 |
| Rust | `///` 文档注释 |

**内容：** 一句话职责 + 必要时说明生命周期（单例 / 无状态 / 线程是否安全）。

### 3.2 字段 / 属性 / 常量

- 对外暴露（public API、配置项、环境变量映射）：**必须**说明含义、单位、默认值约束
- 模块内部 private：**必须**说明用途；显而易见的 getter/setter 可省略

### 3.3 函数 / 方法

- 所有非琐碎函数：**必须**有说明（含 private / internal）
- 参数多、返回值复杂、易误用时：补充每个参数含义、返回值、抛错/错误码
- 异步函数：说明是否可重入、是否幂等、取消/超时行为

| 语言 | 参数/返回值文档 |
|------|-----------------|
| C# | `<param>` `<returns>` |
| Java / Kotlin | `@param` `@return` |
| TypeScript | `@param` `@returns` |
| Python | docstring `Args:` `Returns:` `Raises:` |

### 3.4 关键逻辑块（行内注释）

在以下位置用**简短中文**写意图（1 行为宜）：

- 非显而易见的分支、补偿逻辑、降级
- 性能相关 workaround（N+1、批处理、缓存击穿）
- 安全相关（鉴权绕过禁止、脱敏）
- 与规范相悖的「必须这样写」的 hack（注明原因与 Issue 链接更佳）

---

## 4. 特殊场景约定

### 4.1 路由 / 消息 / 命令分发（switch、路由表、handler map）

每个分支旁注明**产品或协议含义**，而非仅重复枚举名：

```
case "order.paid":    // 支付成功 → 解锁权益并通知履约
case "order.refund":  // 退款完成 → 回滚库存
```

### 4.2 大文件（>300 行或职责过多）

- 文件头 + 运行原理速读：完整
- 每个对外函数 + **重要**内部函数：有文档注释
- 用分区注释分隔：`// --- 校验 ---` / `# --- 持久化 ---`

### 4.3 配置、迁移、生成代码

- 文件头说明：影响哪类环境、生效时机、误改后果
- 生成文件：注明「勿手改，由 XX 命令生成」

### 4.4 测试文件

- 文件头：测什么行为、不测什么（边界）
- 复杂用例：Given / When / Then 一行中文亦可

### 4.5 对外 SDK / 公共 API 包

- 在通用规范基础上，可增加英文 doc，但**运行原理速读**仍建议保留中文供内部协作（按团队约定）

---

## 5. 完整示例

### 5.1 TypeScript 服务模块

```typescript
// =============================================================================
// 文件名称：undoStack.ts
// 所属模块：editor
// 作用说明：
//   维护编辑操作的撤销/重做栈。UI 拖拽结束或快捷键会写入快照，
//   由本模块统一执行 undo/redo 回调。
//
// 【运行原理速读】
//   可以把它想成「编辑器里的后悔药」：
//
//   · 什么时候记一笔？
//     一次用户可撤销操作完成后，保存操作前后的状态快照并压栈。
//
//   · 撤销/重做怎么工作？
//     栈指针指向当前版本；undo 执行快照的恢复函数并后退指针；
//     在中间发生新操作时丢弃指针之后的「未来」分支。
//
//   · 和谁协作？
//     画布组件在变换结束时调用 record；工具栏把 Ctrl+Z 接到 undo()。
// =============================================================================

/** 单条撤销记录：描述 + 撤销/重做回调。 */
interface UndoEntry {
  description: string;
  undo: () => void;
  redo: () => void;
}

/** 撤销栈；最多保留 maxSize 条，超出丢弃最旧记录。 */
export class UndoStack {
  private stack: UndoEntry[] = [];
  private pointer = -1;

  /**
   * 压入一条可撤销记录；若指针不在栈顶，先截断 redo 分支。
   */
  record(description: string, undo: () => void, redo: () => void): void {
    // 指针不在栈顶：清掉「未来」分支（与常见编辑器一致）
    if (this.pointer < this.stack.length - 1) {
      this.stack.splice(this.pointer + 1);
    }
    // …
  }
}
```

### 5.2 Python 脚本

```python
# =============================================================================
# 文件名称：sync_inventory.py
# 所属模块：warehouse
# 作用说明：
#   将 ERP 库存增量同步到电商仓；由调度系统每 5 分钟触发一次。
#
# 【运行原理速读】
#   · 何时跑？Cron 触发，或手动 `--since` 补数。
#   · 做了什么？拉取 ERP 变更 → 幂等写入本地表 → 推送可用库存到商城 API。
#   · 失败？单条 SKU 失败记日志并继续，整批失败 exit 1 供告警。
# =============================================================================

def fetch_changes(since: datetime) -> list[StockDelta]:
    """拉取 ERP 自 since 以来的库存变更；空列表表示无变更。"""
    ...
```

---

## 6. 提交前检查清单

- [ ] 每个源文件有完整文件头（名称 / 模块 / 作用说明 / 【运行原理速读】）
- [ ] 运行原理速读是**原理说明**，不是 IDE/安装/点击流教程
- [ ] 主要类型、字段、函数均有中文说明
- [ ] 注释未改变业务逻辑；lint/编译/测试通过
- [ ] 大文件、路由分支、分区已按第 4 节处理

---

## 7. 给 AI / 协作者的提示词片段（可复制）

```text
请按 CODE_COMMENT_SPEC_GENERIC.md 为以下文件补充中文注释：
1. 文件顶部增加 ============================================================================= 文件头，含【运行原理速读】（讲原理，不讲操作教程）
2. 所有 class/type、字段、函数均有中文文档注释
3. 关键分支加行内中文注释说明意图
4. 只加注释，不修改任何可执行逻辑
```

---

## 8. 修订记录

| 日期 | 说明 |
|------|------|
| 2026-05-28 | 通用版初版：从 Gameza C# 规范抽象，支持多语言注释语法 |
