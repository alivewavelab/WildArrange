# WildArrange 使用说明书

> 本文面向"要把这套流程用起来"的人：先讲怎么装、怎么把自己的 prompt 和规范文档塞进去，再把目前所有工作流按步骤（输入 → 校验 → 做了什么 → 调用了什么 → 输出）逐条列清楚。
>
> **完全新手请先打开可视化手册**：[doc/plans/2026-08-04-beginner-handbook.html](./doc/plans/2026-08-04-beginner-handbook.html)（部署 → 每步怎么塞自己的要求 → 怎么核对门决策对错；含 Cursor / Codex / Kimi 三宿主）。本文是完整参考；那份是按步骤带红绿灯的小白入口。
>
> 只讲"怎么用"，不讲内部实现细节。想看"内部怎么运转、为什么这么设计"（门控模型、Hook 注入机制、多 Agent 原理、追溯兜底）见 [架构解析.md](./架构解析.md)；给维护者的技术地图见 [doc/project-architecture.md](./doc/project-architecture.md)。

---

## 目录

1. [它到底是干嘛的](#1-它到底是干嘛的)
2. [安装](#2-安装)
3. [五分钟跑通一个最小闭环](#3-五分钟跑通一个最小闭环)
4. [如何把 prompt 放入这套流程](#4-如何把-prompt-放入这套流程)
5. [如何把规范文档放入这套流程](#5-如何把规范文档放入这套流程)
6. [如何写计划（plan.json）](#6-如何写计划planjson)
7. [配置文件 helix.config.json（含 Agent 角色与在哪配置）](#7-配置文件-helixconfigjson含-agent-角色与在哪配置)
8. [主线工作流逐步拆解（含每步负责 Agent / 主动被动 / 人话）](#8-主线工作流逐步拆解含每步负责-agent--主动被动--人话)
9. [两个完整范例（端到端走一遍）](#9-两个完整范例端到端走一遍)
10. [会话级注入流程（Hook）](#10-会话级注入流程hook)
11. [多 Agent 并行合入流程](#11-多-agent-并行合入流程)
12. [出问题了怎么办](#12-出问题了怎么办)
13. [命令速查表](#13-命令速查表)

---



## 1. 它到底是干嘛的

WildArrange 把"一次编码请求"变成一条**带门禁的流水线**：

```text
init -> plan -> execute -> verify -> scope -> review -> acceptance-proof -> checkpoint -> resume
```

一句话核心规则：**worker（干活的）可以声称自己完成了，但只有 gate（关卡）才能判定它真的完成。**

它是宿主中立的：可以只用命令行（CLI）跑完整流程，也可以装上 Codex / Cursor / Kimi 的 adapter 让宿主在关键时机自动调用它。

---



## 2. 安装

**环境要求**：Node.js 20 或更高；使用 Git worktree 隔离时需安装 Git。

### 方式 A：从 npm 包直接用（试用）

```bash
npx @alivewavelab/wildarrange@latest init
npx @alivewavelab/wildarrange@latest adapter install
```



### 方式 B：装成项目依赖（推荐长期用）

装成 devDependency，避免每次 hook 都走网络：

```bash
npm i -D @alivewavelab/wildarrange
npx wildarrange adapter install --mode local
```



### 方式 C：在本仓库内本地开发

```bash
node ./bin/helix.mjs init
node ./bin/helix.mjs adapter install --target all --mode local
```

> 下文命令统一用本地形式 `node ./bin/helix.mjs ...`。如果你是方式 A/B，把它替换成 `npx wildarrange ...` 即可。



### 安装做了什么

- `init`：创建运行时目录 `.helix/`，写入默认配置，安装内置 prompt 包（记录每个 prompt/skill 的哈希指纹）。
- `adapter install`：
  - **Codex**：把生命周期 hook 写入项目的 `.codex/hooks.json`，并在 `.helix/adapters/codex/hooks.json` 留审计副本。需要在 Codex 里通过 `/hooks` 信任后才会硬拦截。trust 之前 hook 不生效；trust 后仍不能替代 verifier / scope / review / acceptance proof / checkpoint。
  - **Cursor**：写入 `.cursor/hooks.json`（含 hook bridge）。受信任工作区中 `preToolUse`（Write/Delete/Edit/StrReplace/MultiEdit/Shell）与 `beforeShellExecution` **fail-closed 硬拦截**；`.cursor/rules/wildarrange.mdc` 仍是软规则层。
  - **Kimi**：生成 `.helix/adapters/kimi/plugin/`。从项目根启动 Kimi Code 后执行 `/plugins install .helix/adapters/kimi/plugin` 与 `/reload`（路径勿加引号）。Hook 崩溃或超时 **fail-open**；最终完成仍以 verifier / scope / review / acceptance proof / checkpoint 为准。
  - **三端都会生成一组 slash / skill 命令**（见下），省得你每次开终端敲 `node ...`。



### 生成的 slash 命令（省去开终端）

`adapter install` 会按宿主生成同一套命令：

- **Cursor**：`.cursor/commands/*.md`，在聊天里输入 `/helix-doctor` 这样触发。
- **Codex / Kimi**：`.agents/skills/helix-*/SKILL.md`，用 `/skills` 或 `$helix-doctor` 触发（Codex 0.117 起用 skill 取代了自定义 prompts）。


| 命令               | 作用                                                                 |
| ---------------- | ------------------------------------------------------------------ |
| `/helix-config`  | 生成 `helix.config.json` 并逐块引导填写，最后 `config verify` 校验               |
| `/helix-doctor`  | 一键体检：`doctor` + `config verify` + `ledger verify` + `state verify` |
| `/helix-refresh` | 新增/改了 prompt/skill 后刷新运行时（幂等，不清任务与账本）                              |
| `/helix-status`  | 看进度、下一步、失败任务与待办                                                    |
| `/helix-plan`    | 导入并校验一个 `plan.json`                                                |
| `/helix-approve` | 展示计划摘要并请你确认，确认后才放行执行（开启确认门时用）                                      |
| `/helix-run`     | 跑下一个任务，走完整门禁                                                       |


> 本质：每个命令是一段提示词，让 AI **代你执行**对应的 `helix.mjs` 命令并汇报结果——不是原生按钮，而是"让 AI 帮你敲 CLI"。所以下文那些 `node ./bin/helix.mjs ...` 命令，你既可以自己敲，也可以用对应 slash 命令让 AI 代跑。

---



## 3. 五分钟跑通一个最小闭环

新建 `plan.json`：

```json
{
  "title": "Todo app smoke",
  "objective": "Write one verifiable artifact",
  "tasks": [
    {
      "id": "T001",
      "subject": "Write smoke artifact",
      "writable_paths": [".helix/artifacts/smoke.txt"],
      "worker_command": "node -e \"const fs=require('fs'); fs.mkdirSync('.helix/artifacts',{recursive:true}); fs.writeFileSync('.helix/artifacts/smoke.txt','ok\\n')\"",
      "verify_commands": ["node -e \"const fs=require('fs'); if(fs.readFileSync('.helix/artifacts/smoke.txt','utf8').trim()!=='ok') process.exit(1)\""],
      "review_commands": ["node -e \"const fs=require('fs'); if(!fs.readFileSync('.helix/artifacts/smoke.txt','utf8').includes('ok')) process.exit(1)\""]
    }
  ]
}
```

> `review_commands` 不能省：验收证明会拒绝「没有任何独立复核信号」的任务进入 completed。独立信号可以是 `review_commands` / `standards_commands` / `review.llm` / 已启用的质量门之一。

运行：

```bash
node ./bin/helix.mjs plan --from plan.json   # 导入并校验计划
node ./bin/helix.mjs run                      # 跑下一个任务（全程过 gate）
node ./bin/helix.mjs status                   # 看状态
node ./bin/helix.mjs summary                  # 看总结
```

或者直接跑内置样例（不用自己写 plan）：

```bash
node ./bin/helix.mjs workflow --sample
```

---



## 4. 如何把 prompt 放入这套流程

这套流程用一个 **prompt 包（prompt pack）** 管理所有角色提示词、技能提示词和工具合同。内置包在 `packs/wildarrange-linear/`，结构：

```text
packs/wildarrange-linear/
├── manifest.json          # 清单：声明有哪些 agent / skill / tools / routes
├── agents/                # 五个长期角色提示词（Jiuwei、DiJiang、ZhuRong、BaiZe、LuWu）
├── skills/                # 技能提示词（wa-plan、review-work、debugging…）
├── tools/tool-contract.json
└── routes.json            # 路由关键词表
```



### 4.1 新增或修改一个技能 prompt

1. 在 `packs/wildarrange-linear/skills/` 放一个 Markdown 文件，例如 `my-skill.md`。
2. 在 `manifest.json` 的 `skills` 里登记它：

```json
{
  "skills": {
    "my-skill": "skills/my-skill.md"
  }
}
```

1. 刷新登记。**这一步很轻，不是危险的重置**：`init` 是幂等的，只补建缺失目录、重新登记 prompt 包，**不会动你的任务和账本**。而且几乎每个命令、每次 hook 触发都会自动重新登记，所以多数情况你不用特意做这步。想显式触发就用 `/helix-refresh`，或：

```bash
node ./bin/helix.mjs init
```

> 两种情况要分清：
>
> - **改已登记 skill 的正文** → 注入时直接读你的原文件，**改完立即生效**，连刷新都不用。
> - **新增一个新文件名的 skill** → 只要在 `manifest.json` 登记，下一次任何操作（下条命令 / 下次 hook）就自动纳入。
>
> 登记后运行时会记录每个 prompt 文件的哈希，用于防篡改。



### 4.2 让某个技能在特定节点被挂载

在 `helix.config.json` 的注入点上声明它（见 [第 7 节](#7-配置文件-helixconfigjson) 的 `injectionPoints`）。这里配置的是**候选清单（上限）**。

技能挂载默认是**按需**的（`skillMatcher.dynamicInjection`）：

- 有请求文本时，只有**和本次请求匹配**的候选技能才注入全文；不匹配的降级成"一行引用 + 加载命令"，Agent 需要时自己拉。
- `alwaysMount`（默认 `wildarrange-injection-runtime`）永远注入。
- `maxSkills`（默认 4）限制单次最多挂几个。
- 动态匹配**只做减法**：绝不会把候选清单之外的技能全文塞进来，哪怕请求里堆满关键词。

手动看某个请求会匹配到哪些技能：

```bash
node ./bin/helix.mjs skills match --text "做一个网页版提醒事项 App" --stage design --agent Jiuwei
```



### 4.3 用自己的整包替换内置包

`init` 支持指定 pack 目录（编程接口 `initRuntime(dir, { promptPackDir })`）。想整包替换时，复制 `packs/wildarrange-linear/` 为自己的目录、改好 `manifest.json`，再让运行时指向它即可。默认不指定就用内置包。

### 4.4 提示词变体（按模型追加偏置，不替换原 prompt）

```bash
node ./bin/helix.mjs prompts variant --agent Jiuwei --model gpt-5.5
node ./bin/helix.mjs prompts show --agent Jiuwei --variant gemini
```

变体只是**追加**在原始 Agent prompt 后面的一小段模型偏置，不会替换原 prompt。GPT 系列 / Codex / Cursor 主模型默认走 `host` / `gpt`。

---



## 5. 如何把规范文档放入这套流程

> **先分清两类东西，别混：**
>
> - **编码规范 / 前后端约束** → 放**规则文件**（本节），按路径 `globs` 命中，按需注入。
> - **工作流 / 作业指导** → 放 **prompt 包的 skills**（第 4 节），按阶段/请求匹配挂载。
> - 你的编码规范应该放进规则文件，**不是 skills**。

规范文档（你项目里的"编码规范""验收要求""目录约定"等）通过**规则扫描**进入流程。运行时会自动扫描这些位置：

**单文件：**

- `AGENTS.md`
- `CLAUDE.md`
- `CONTEXT.md`
- `.github/copilot-instructions.md`

**规则目录（目录内所有规则文件）：**

- `.claude/rules/`
- `.cursor/rules/`
- `.github/instructions/`



### 5.1 全局规范：直接写进 AGENTS.md / [CLAUDE.md](http://CLAUDE.md)

放"永远生效"的硬约束（红线、目录约定、命令表）。这些默认全命中。

### 5.2 按前后端 / 文件类型生效的规范：用带 globs 的规则文件

匹配依据是**本次任务要改动的文件路径**（任务的 `writable_paths` + git 实际改动路径），不是你在编辑器里打开的 tab。两种写法都支持：

**A. 靠路径/目录判断前后端**（路径本身分了前后端）：

```markdown
---
description: 前端组件规范
globs: [src/frontend/**, apps/web/**]
---

前端改动必须遵守组件规范，并附浏览器验收截图。
```

**B. 靠文件后缀判断**（路径没有前后端标注时）：

```markdown
---
description: 后端接口规范
globs: [**/*.go, **/*.py, server/**]
---

后端改动必须遵守接口契约与错误处理规范。
```

只有当改动路径命中 `globs` 时，这条规范才会被注入——这就是"按需加载"：**改后端不塞前端规范，改前端才塞前端规范。** 没写 `globs`（或声明 `alwaysApply`）的规则则全局生效。

### 5.3 手动查看某次改动会命中哪些规范

```bash
node ./bin/helix.mjs rules collect --target src/app.js
```

它会把命中的规范写入 `.helix/rules/context.md` 和 `context.json`。规范内容有字符预算，超长会显式标记截断，不会静默丢。

> 注意：`AGENTS.md`、`CLAUDE.md` 这类控制面文档本身默认是**只读**的（见 CLAUDE.md 硬约束），你可以往里写规范，但流程不会去改它们。



### 5.4 直接用现成模板起步

不想从零写？`examples/fullstack-starter/` 是一套**带注释的空架子模板**：全局红线（`AGENTS.md`）、前端/后端/数据库三份带 `globs` 的规则文件、一份可直接跑通的 `plan.example.json`，以及带逐块注释的完整配置讲解。复制到你项目根，照着 `examples/fullstack-starter/README.md` 的自检清单先跑通一次，再逐块填规范即可。任务依赖字段必须写 `blockedBy`，不要写 `dependsOn`（后者会被忽略）。

---



## 6. 如何写计划（plan.json）

计划是整条流水线的入口。一个任务（task）的常用字段：


| 字段                   | 必填    | 说明                                       |
| -------------------- | ----- | ---------------------------------------- |
| `id`                 | 否     | 任务 ID，不填自动生成 `T001`…                     |
| `subject`            | **是** | 任务标题（`title` 也可）                         |
| `description`        | 否     | 任务详述，默认等于 subject                        |
| `writable_paths`     | 强烈建议  | 允许改动的路径白名单（支持 glob）。**范围守卫**据此判定越界       |
| `worker_command`     | 否     | 干活的命令（真正产出改动）                            |
| `verify_commands`    | **是** | 至少一条验证命令，全部 exit 0 才算通过                  |
| `review_commands`    | 导入否 / 完成是 | 独立复核命令。导入可不写，但无独立复核信号（本字段 / `standards_commands` / `review.llm` / 已启用质量门）则无法 completed |
| `standards_commands` | 否     | 规范检查命令                                   |
| `successCriteria`    | 否     | 验收标准；不填会按 verifier 生成默认三条（主路径 / 边界 / 回归） |
| `blockedBy`          | 否     | 依赖的前置任务 ID，构成 DAG                        |
| `maxAttempts`        | 否     | 最大自动重试次数，默认 3                            |
| `skills`             | 否     | 本任务建议激活的技能                               |


顶层还可以写 `defaults`（对所有任务生效的默认 `verify_commands` / `writable_paths` / `skills` 等）。

> 计划导入时会做严格校验（见 [第 8 节 步骤 1](#步骤-1plan导入产品需求--结构化计划)）。**没有** `writable_paths`**、**`worker_command` **空、**`verify_commands` **又只是** `true` **/** `process.exit(0)` **这种空转任务，会被标记** `possible_noop_task` **告警，并在验收阶段被硬拦。**

---



## 7. 配置文件 helix.config.json（含 Agent 角色与在哪配置）

生成一份可编辑的根配置：

```bash
node ./bin/helix.mjs config init --root
```

关键配置块：


| 配置块                             | 作用                                                    |
| ------------------------------- | ----------------------------------------------------- |
| `agents`                        | 每个角色用哪个 provider / model / reasoning                  |
| `modelProviders`                | 模型 provider。`host` 交给 Codex/Cursor；外部走 OpenAI 兼容 HTTP |
| `injectionPoints`               | 每个注入点挂哪些 `tools` / `markdown` / `skills` / `rules`    |
| `contextBudgets`                | Prompt / Markdown / Skill 的字符预算，超了显式标 `truncated`     |
| `skillMatcher.dynamicInjection` | 技能按需挂载：`enabled` / `maxSkills` / `alwaysMount`        |
| `qualityGates`                  | LSP/类型检查、AST 结构检查、hashline anchor、注释检查                |
| `review.llm`                    | 是否启用 LLM 复核；`required=false` 时无 key 只告警不阻断            |
| `commandSafety.extraPatterns`   | 在内置高危正则之上**追加**项目专属危险命令拦截（见下）                         |
| `planApproval.required`         | 打开后，导入的计划必须开发者确认才能 `run`（见第 8 节 · 步骤 0.5）             |
| `archivistRouter`               | 是否启用档案路由员                                             |
| `routeGovernance`               | 路由治理，可配语义第二意见                                         |


`**commandSafety.extraPatterns`（外置危险命令规则）**：内置的高危命令正则（`**rm -rf /`**、**`git reset --hard` **等）是**不可关闭的底线**；你只能在它之上**加规则**，不改代码：

```json
{
  "commandSafety": {
    "extraPatterns": [
      { "id": "no_prod_deploy", "pattern": "deploy\\s+--env\\s+prod", "flags": "i", "reason": "生产部署必须走人工流程" }
    ]
  }
}
```

命中的 worker / verify / review 命令会被直接拦下（退出码 126）。`pattern` 是正则字符串，`flags` 默认 `i`，写错的正则会被安全跳过。

### 7.1 Agent 角色表：这套流程里都有谁

流水线不是一个 AI 从头干到尾，而是**分角色**：谁编排、谁干活、谁验收、谁挑刺是分开的（这就是"worker 不能自证完成"的由来）。默认角色如下（都在 `agents` 块里配）：


| Agent      | 角色                    | 大白话：它管啥                  |
| ---------- | --------------------- | ------------------------ |
| `Jiuwei`   | workflow_orchestrator  | 总指挥兼线性执行官：派发任务并推进交付闭环 |
| `DiJiang`  | planner               | 计划者：澄清目标、拆任务、定义范围与验收 |
| `ZhuRong`  | implementation_worker | 干活的：真正改代码/写文件（worker）    |
| `BaiZe`    | independent_reviewer  | 唯一独立复核者：查目标、证据、风险与验收 |
| `LuWu`     | repository_steward    | 只读仓库治理：查目录、文档、命名、归属和注释 |


> 范围守卫（scope）、验收证明、checkpoint 这些是**系统 gate（规则程序）**，不是 Agent——它们不讲道理，只按硬规则判定，防止任何 Agent（包括总指挥）绕过。Router 是确定性系统节点，不占 Agent 名额；CangJie 是可选内部档案/语义路由配置。



### 7.2 在哪配置 Agent

在 `helix.config.json` 的两个块里配：

- `**agents**`：每个角色用哪个 provider、哪个 model、什么推理强度。例如：

```json
{
  "agents": {
    "Jiuwei": { "role": "workflow_orchestrator", "provider": "host", "model": "host-default", "reasoning": "high" },
    "BaiZe":  { "role": "independent_reviewer", "provider": "host", "model": "host-default", "reasoning": "xhigh" },
    "LuWu":   { "role": "repository_steward", "provider": "host", "model": "host-default", "reasoning": "high" }
  }
}
```

- `**modelProviders**`：这些 model 从哪来。`provider: "host"` 表示**直接用 Codex/Cursor 当前的主模型**（最省事，不用配 key）；外部模型走 OpenAI 兼容 HTTP，`apiKeyEnv` 填**环境变量名**（不是密钥本身）。

> 想省事：5 个长期 Agent 全部使用 `provider: "host"`，让编辑器主模型按静态 Prompt/Skill 切换职责即可。`CangJie` 仅是可选内部档案/语义路由配置，可单独指向便宜模型。改完用 `/helix-config` 或 `node ./bin/helix.mjs config verify` 校验。

配置真相源优先级：`helix.config.json` > `.helix/config.json` > 运行时默认。查看最终生效配置：

```bash
node ./bin/helix.mjs config show
```

外部 provider 用 OpenAI 兼容配置，`apiKeyEnv` / `baseUrlEnv` 填的是**环境变量名**不是密钥本身。确定性 gate 不依赖模型 API，缺 key 时线性状态机照常跑。

---



## 8. 主线工作流逐步拆解

这是你最关心的部分：从"产品需求进来"到"存档"，每一步**谁负责、你要不要动手、输入了啥、校验了啥、干了啥、输出了啥**。

**先约定两个标记，帮你快速判断"要不要我操心"：**

- **触发 = 主动**：需要开发者（或 AI 代你）**主动敲命令/回答**才会发生（比如导入计划、确认计划、发起 run）。
- **触发 = 被动**：`run` 一旦发起后**自动串下去**，或宿主在事件时**自动注入**，你不用管。

对应命令：`plan` 导入后，`run` 会自动跑步骤 2→6；也可以用 `node ./bin/helix.mjs node <阶段> --task <id>` 单步跑（`execute|verify|scope|review|checkpoint|retry`）。没有单独的 `node acceptance-proof`；`run` 与 `node checkpoint` 在 checkpoint 前会自动跑验收证明。

> **关于"需要你拍板"的三件事，都走同一个通道——AI 主动在对话里问你。**
> 系统不绑飞书/邮件等任何外部推送。所有"待你决策"的事项（计划待确认、改动越界待审、任务失败、子 Agent 待验收）会由 hook 在 SessionStart / 你发消息 / 停止 等时机，**注入到宿主 AI 的上下文并要求它主动向你复述、给出选项、等你答复**。你只要在对话里回答即可，AI 会代你执行对应命令。用 `node ./bin/helix.mjs status` 或 `/helix-status` 也能随时拉取这份待办清单。



### 步骤 0.5：计划确认门（可选，默认关闭）

- **负责**：开发者拍板 + AI 复述计划。**触发：主动**（你回答"确认"）。
- **开关**：`helix.config.json` 的 `planApproval.required`。默认 `false`（导入即可 `run`）。
- **打开后**：`plan --from` 导入完成后，计划进入 `awaiting_plan_approval`；此时 `run` 会**拒绝执行**并返回 `awaiting_plan_approval`，直到你确认。
- **怎么确认**：在对话里用 `/helix-approve`——AI 会先复述计划要点并问你"确认 / 需要修改"，你说确认后它执行 `plan approve`；或你自己敲 `node ./bin/helix.mjs plan approve`。
- **为什么**：让"计划从产品/技术文档提炼"这一步有一道人工闸门，避免 AI 拿着没对齐的计划直接开跑。

---



### 步骤 0：init（初始化环境）

- **负责**：系统（开发者敲命令或装 adapter 时触发）。**触发：主动**。
- **输入**：无（可选 `--promptPackDir` 指定自定义 prompt 包）。
- **校验**：无强校验。
- **干了啥**：创建 `.helix/` 目录骨架；写默认配置；安装 prompt 包并给每个 prompt/skill 记录哈希指纹；写初始快照。
- **调用**：`init` → `initRuntime` → `installPromptPack`。
- **输出**：`.helix/` 运行时目录、`.helix/prompt-pack.json`、账本事件 `config_written` / `runtime_initialized`、`snapshot(initialized)`。

---



### 步骤 1：plan导入（产品需求 → 结构化计划）

- **负责**：`DiJiang`（计划者）主导澄清与拆任务，`wa-spec`/`wa-plan` 技能引导 AI 从产品/技术文档提炼计划；`Jiuwei` 负责导入后的编排推进；`CangJie`（档案路由）顺带召回历史踩坑。**触发：主动**（你发需求、AI 生成并执行 `plan --from`）。
- **输入**：`plan.json`（产品需求已经拆成 `title` / `objective` / `tasks[]`）。
- **校验**（导入时逐层把关）：
  1. `title` 必填、`tasks` 至少一个；
  2. 每个任务必须有 `subject`，且至少一条 `verify_commands`；
  3. `successCriteria` 结构合法，`verifierCommandRefs` 必须指向真实存在的验证命令；
  4. **DAG 校验**：任务 ID 不重复、`blockedBy` 引用存在、不能自依赖、不能成环；
  5. **高风险产品计划质量门**：如果内容命中产品类关键词（产品/用户/页面/网页/上传/PDF/游戏/MVP/权限/协作…）且路由判为高风险或多 planAgent，则强制要求 **≥4 个任务**且**必须有一个验证/复核类任务**，否则拒绝导入。
- **干了啥**：归一化每个任务（合并 `defaults`、生成默认 `successCriteria`、给空转任务打 `possible_noop_task` 告警）；做路由 enrichment，给整个计划和每个任务打 `route_decision` / `category` / `skills`。
- **调用**：`plan --from` → `importPlan` → `normalizePlan` / `validatePlanGraph` → `enrichPlanWithRoutes`（`resolveRouteDecision`）→ `validatePlanImportQuality`。
- **输出**：`.helix/plans/<planId>.json`、`.helix/team/tasks.json`（任务状态真相源）、`.helix/team/tasks.md`（人读 TODO）、`work.json`（stage=planned）、账本事件 `plan_imported` / `plan_routed`、`snapshot(planned)`。

---



### 步骤 2：execute（选下一个任务并干活）

- **负责**：`Jiuwei`（总编排兼线性执行官）推进节奏，`ZhuRong`（worker）真正改代码。**触发：主动发起** `run`**（或** `/helix-run`**）后，本步及后续 3→6 自动串下去（被动）**。
- **输入**：`tasks.json` 里下一个**可运行**任务（状态 `pending` 且它依赖的前置任务都已完成）。
- **校验**：`worker_command` 先过**命令安全预检**——删 `.git`/`.helix`、递归删 `src`/`test`/`doc`、`git reset --hard`、`git clean -fd`、`sudo`、`curl | sh` 等破坏性命令会被直接阻断。
- **干了啥**：任务置 `in_progress`、`attempts +1`；**执行前先记录一份工作区快照**（`git stash create`，用于事后还原）；采集执行前的 diff 和改动路径；运行 `worker_command`；再采集执行后的 diff 和改动路径。
- **调用**：`run` / `node execute` → `recordPreExecuteSnapshot`（`captureWorkspaceSnapshot`）→ `runWorker`（含命令安全）→ `collectGitDiff` / `collectGitChangedPaths`。
- **输出**：任务状态 → `verifying`；证据里追加"工作区快照 / worker 结果 / diff / 改动路径"；写 outbox；账本事件 `pre_execute_snapshot` + `worker_done_claim`（或 `node_execute_completed`）；快照。
- **人话**：这一步只是"干活的说自己做完了"，还没人核对。工作区快照 = 动手前拍的存档照片，万一改坏了能一键还原。
- **关键点**：worker 退出码为 0 **只是一个"声称完成"**，不代表任务完成。

---



### 步骤 3：verify（独立验证）

- **负责**：系统 verifier（`runVerifier` capability）。**触发：被动**（`run` 自动进行）。BaiZe 不跑这一步，只在步骤 5 复核门出场。
- **输入**：任务 + `verify_commands`。
- **校验**：逐条运行 `verify_commands`，**全部 exit 0** 才 `pass`；任一非 0 即 `fail`。
- **干了啥**：把 verifier 结果自动映射到那些绑定了 `verifierCommandRefs` 的验收标准（`successCriteria`），把对应标准从 pending 推进到有证据。
- **调用**：`node verify` → `runVerifier`（含命令安全）→ `applyVerifierEvidenceToCriteria`。
- **输出**：`verifyResult`（pass/fail）、`task.last_verify_result`、证据、账本 `node_verify_completed`；若 `fail` → 任务回 `pending` 或 `failed` 并写失败报告 + `node_verify_failed`。

---



### 步骤 4：scope（范围守卫）

- **负责**：系统 gate（规则程序，不讲情面）。**触发：被动**。
- **输入**：本次任务**引入的改动路径**（执行后 − 执行前）+ 任务的 `writable_paths`。
- **校验**：所有新增/改动路径必须落在 `writable_paths` 白名单内（用 realpath 防目录穿越）。有任何越界 → `fail`。拿不到 Git 改动信息时返回 `inconclusive`（不算通过）。
- **干了啥**：一旦越界，自动生成一份 **ChangeRequest（变更申请）**，记录越界路径，等人审。
- **调用**：`node scope` → `scopeGuard` →（越界时）`writeChangeRequest`。
- **输出**：`scopeResult`（pass/fail/inconclusive）、`task.last_scope_result`、越界时 `last_change_request`、账本 + 快照。

---



### 步骤 5：review（复核门）

- **负责**：`BaiZe` 作为唯一独立复核者，按需挂载目标/证据、bug/风险和怀疑式验收 Skill，再结合确定性检查。**触发：被动**。
- **输入**：worker / verify / scope 的结果 + `review_commands` + `standards_commands` + `qualityGates` + 可选 LLM 复核。
- **校验**：多条 lane 一起判：
  - 确定性 lanes：目标合规、QA 执行、代码审查、安全审计、上下文挖掘；
  - 配置的 `review_commands` / `standards_commands`；
  - `qualityGates`：LSP/类型检查、AST 结构检查、hashline anchor、注释检查；
  - 可选 LLM 复核（启用时）：BaiZe 使用相应 Review Skill 综合检查目标/证据、bug/风险和怀疑式验收。
  - **判定规则**：主 lane 必须 PASS；安全出现 high/critical 直接阻塞；证据不足只能 `inconclusive`（**不算通过**）。
- **调用**：`node review` → `runReviewGate`。
- **输出**：`reviewResult`、复核报告（`.helix/reports/reviews/<planId>-<taskId>.json`）、账本 `review_gate_completed`、快照。

---



### 步骤 6：acceptance proof + checkpoint（验收证明 + 存档）

- **负责**：系统 gate（把前面所有 Agent 的结论再对一遍账）。**触发：被动**。
- **输入**：前面所有 gate 的结果。
- **校验（两道）**：
  1. **准入门**：必须同时满足 `worker exit 0` **且** `verify pass` **且** `successCriteria pass` **且** `scope pass` **且** `review pass`，才允许进入验收证明；
  2. **验收证明**：再逐项核验 worker / verifier / successCriteria / scope / review / review lanes 的证据链，并拒绝 no-op、trivial verify（全是 `true`）和同义反复 review（没有任何独立复核信号）。
- **干了啥**：证明通过 → 任务置 `completed`、写 checkpoint、追加 wisdom、写完成 digest；证明不过 → 回 `pending` / `failed` 并写失败报告。
- **人话（重要）**：
  - **通过时**：这个任务算**真正做完了**——存一个"存档点"（checkpoint，日后能精确回溯到这一刻）、把这次学到的经验记进**经验库**（wisdom，下次遇到类似活儿能少踩坑）、再写一份**完成小结**（digest，下次续跑时 AI 一看就知道之前干到哪了）。
  - **没通过时**：任务被**打回重做**（`pending`）或**标记失败**（`failed`），并生成一份**失败报告**，写清楚是哪一关没过、缺什么证据。
- **调用**：`node checkpoint`（或 `run` 尾段）→ `writeAcceptanceProof` → `writeCheckpoint` → `appendWisdom` → `writeMemoryDigest`。
- **输出**：`.helix/reports/acceptance/<planId>-<taskId>.json`、`.helix/checkpoints/<planId>-<taskId>.json`、账本 `task_verified` / `node_checkpoint_completed`、完成 digest、`snapshot(checkpointed)`；当所有任务都完成时额外生成 workflow 总结。

---



### 步骤 7：retry（失败返工）

- **负责**：`Jiuwei` 重新推进 + 系统 gate 把关。**触发：主动**（你或 AI 发起 `node retry`）。
- **输入**：`failed` / `pending` 的任务 + 它的 `last_failure`。
- **校验**：如果失败原因是**范围越界**（`scope_guard_failed`）且没加 `--force`：
  - 必须先有一份**已 accept 的 ChangeRequest**，否则返回 `change_request_required`（挡住，去审变更申请）；
  - 即使已 accept，当前越界路径**仍未清理**也会返回 `scope_cleanup_required`。
- **干了啥**：确认清理后，把任务重置为 `pending`、`manual_retry_count +1`、抬高 `maxAttempts`，等待下一轮 `run`。
- **调用**：`node retry` → `retryTaskNode` →（按需）`readChangeRequest` / `scopeGuard`。
- **输出**：任务 → `pending`、账本 `node_retry_reopened`（被挡则 `node_retry_blocked`）、快照。

> 贯穿全程：每一步都会往 `.helix/ledger.jsonl`（带哈希链的账本）追加事件，往 `.helix/snapshots/context.md` 写恢复快照。任何一步都能被追溯、被恢复。

---



## 9. 两个完整范例（端到端走一遍）



### 范例 A：从零做一个「短信记账」App 的 1.0

**小 A 的目标**：截图短信里的扣款通知 → 自动识别金额/商户/时间 → 记进账本 App。

**① 澄清（对话里，主动）**
小 A 打开 Cursor，说："我想做个 App，把银行扣款短信截图丢进去，自动记成一笔账。"

`wa-spec` 技能被挂进上下文，AI 触发**澄清纪律**——它不会闷头就写，而是先反问关键缺口：

- 平台是手机 App 还是网页？先支持哪家银行的短信格式？
- "识别"是本地 OCR 还是调用云端接口？（涉及隐私和联网）
- 账本存本地还是云端？1.0 要不要登录？

小 A 回答："先做网页版 demo，只认一种短信格式，OCR 用现成云接口，账本先存本地 JSON，不做登录。"

**② 拆任务 + 给你确认（主动）**
计划者（`DiJiang`）据此拆出一份计划，总指挥（`Jiuwei`）负责导入后的推进；`CangJie` 顺带提醒"OCR 接口 key 别写进代码"。因为开了**计划确认门**，计划先进入 `awaiting_plan_approval`，AI 在对话里复述：

> 计划共 5 个任务：① 搭页面骨架与上传框；② 接 OCR 接口把图转文字；③ 解析文字里的金额/商户/时间；④ 存进本地账本并渲染列表；⑤ 验收任务（跑一遍样例截图，核对结果）。确认开跑吗？

小 A 说"确认"。AI 代他执行 `plan approve`，计划解锁。（没开确认门就直接能 `run`。）

**③ 写代码前，其他 Agent 收到了啥（被动注入）**
小 A 发起 `run`。轮到任务①时：

- **总编排兼线性执行官**（`Jiuwei`）拿到"当前任务上下文"——目标、可写目录 `writable_paths`（比如只允许改 `src/`）、命中的**前端规范**（因为改动路径是前端代码，规则按路径/后缀自动命中并注入）。
- 动手前系统先**拍一张工作区快照**（改坏了能还原）。

**④ 怎么写、返回啥（干活→自动串验收）**

- 干活的（`ZhuRong`）执行 worker，改出页面文件，退出码 0——**但这只是"声称完成"**。
- 系统自动接着跑：**独立验证**（系统 verifier 跑 `verify_commands`，比如构建/单测过没过）→ **范围守卫**（改的文件有没有越出 `src/`）→ **复核门**（`BaiZe` 按需加载风险与怀疑式 Review Skills）→ **验收证明 + 存档**。
- 全过 → 任务①算**真正完成**：存档点 + 记经验 + 写完成小结。任一关没过 → 打回重做并出失败报告。
- 任务②③④⑤依次同样走一遍；任务⑤（验收）跑样例截图核对识别结果，作为整体质量的兜底。

**⑤ 小 A 怎么验证（主动）**

- 看待办与进度：`/helix-status` 或 `node ./bin/helix.mjs status`。
- 全部完成后 AI 生成 workflow 总结；小 A 打开网页，丢一张真实短信截图，确认能正确记成一笔账。
- 想追溯某一步：账本 `.helix/ledger.jsonl` 有全过程留痕，快照可回滚。

至此 1.0 完成。

### 范例 B：1.0 之后，做一个新功能的版本更新

**新需求**：给账本加一个「本月支出饼图」。

和从零做最大的不同是：**大量东西可以复用，AI 会先"回忆"**。

**① 增量澄清（主动）**
小 A 说："给账本加个本月支出的分类饼图。"

- `CangJie`（档案路由）先**召回 1.0 的完成小结和经验库**：账本数据存在哪个本地 JSON、用了什么前端框架、上次踩过哪些坑——这些直接进上下文，AI 不用重问已知信息。
- `wa-spec` 只针对**增量**澄清："按商户分类还是按自定义标签？只统计本月还是可切月份？"小 A 定："按标签分类，先只做本月。"

**② 只拆"改动部分"的计划 + 确认（主动）**
`Jiuwei` 拆出一份小计划（例如 3 个任务：加聚合统计函数、加饼图组件、验收任务），并**尽量只圈定相关模块**作为 `writable_paths`，不动 1.0 已稳定的解析逻辑。AI 复述计划，小 A 确认。

**③ 执行（被动串下去，和范例 A 相同）**

- 改动落在前端图表目录 → 命中的仍是**前端规范**；如果新增了后端聚合接口，改到后端目录时会**自动切换命中后端规范**。
- 每个任务照样走"执行→独立验证→范围守卫→复核→验收存档"，worker 依旧不能自证完成。
- 因为范围守卫盯着 `writable_paths`，如果 AI 手滑去改了 1.0 的核心文件，会被**当场拦下并生成变更申请（ChangeRequest）**，推到对话里等小 A 拍板，而不是偷偷改掉。

**④ 验证与存档（主动查看）**

- `/helix-status` 看新任务进度；打开页面确认饼图正确。
- 新功能完成后同样写入存档点和经验库，成为下一次迭代可召回的历史。

> **一句话对比**：1.0 是"从空白澄清 → 拆全量计划 → 逐个建"；版本更新是"先召回历史 → 只澄清增量 → 拆小计划 → 只改相关模块（范围守卫护住老代码）"。骨架流程完全一致，区别在**复用记忆**和**收窄改动范围**。

---



## 10. 会话级注入流程（Hook）

装了 adapter 后，宿主编辑器会在关键时机（会话开始、你发消息、工具调用前后、上下文压缩、停止）**自动**调用 `hook run`，把该挂的规则/技能/状态拼进上下文——这是**被动注入**，你不用手动触发。

宿主 Hook 事件会映射到多个**注入点**（默认还含 `before_execute` / `before_review` / `before_checkpoint` 等）。完整列表见 `config show` 的 `injectionPoints`；六个宿主事件映射、动态技能挂载、上下文预算和通用推送见 [架构解析.md §4](./架构解析.md#4-会话级注入机制hook)。这里只给你调试时会用到的**手动喂一个 hook payload** 的方式：

```bash
node ./bin/helix.mjs hook run --from hook.json
```

`hook.json` 最小格式：

```json
{
  "hook_event_name": "UserPromptSubmit",
  "session_id": "session-1",
  "cwd": "/path/to/project",
  "prompt": "用户当前请求"
}
```

---



## 11. 多 Agent 并行合入流程

命令型子 Agent 可以在隔离目录或 Git worktree 里并行干活，但**同样不能自证完成**。`--agent` 只能是可写身份（`Jiuwei` / `ZhuRong`）；`DiJiang`、`BaiZe`、`LuWu` 不得进入 command worker。

```bash
# 1. 并行跑子 Agent（隔离运行目录）
node ./bin/helix.mjs parallel run --max-agents 2 --task T001,T002 --agent ZhuRong --command "..."

# 或用 Git worktree 隔离
node ./bin/helix.mjs parallel run --task T001 --isolation git-worktree --command "..."

# 2. 查看运行记录
node ./bin/helix.mjs parallel status --run <runId>

# 3. 合入主线（会走完整 gate，不直接信任子 Agent）
node ./bin/helix.mjs parallel admit --run <runId> --task T001

# 4. 用户验收后显式关闭保留结果
node ./bin/helix.mjs parallel close --run <runId> --task T001 --reason user_accepted
```

流程要点：

1. 子 Agent 把结构化成果写进 `agent-result.json`（`files[]` 或 worktree `patch`）。
2. `parallel admit` 先检查 `writable_paths`，越界直接拒。
3. 通过后把文件/patch 应用到主工作区，再依次跑 **verifier → 范围守卫 → 复核门 → 验收证明 → checkpoint**，全过才能 `completed`。
4. 合入失败会**回滚**主工作区的改动，子 Agent 结果保留待修。
5. 成功的子 Agent 结果先进入 `awaiting_user_acceptance`，admission 完成后才变 `released`。
6. 回滚失败会保留 `verifying` claim 与 rollback plan，返回 `recovery_required`，必须由原 run 恢复。
7. 有 Git remote 且 `gitCoordination` 为 `guarded`/`strict` 时：admission 先绑定远端集成分支 SHA，gate 期间 owner/SHA 变化或存在无归属改动会安全回滚并返回 `revalidation_required`；全部 gate 与验收证明通过后必须生成以该 SHA 为父提交的集成 commit 并普通 push，成功后才允许 checkpoint。push 已成功后不得回滚原 run。

### 跨设备与 Git 协调

多设备默认 `guarded`：有 remote 时用任务分支 claim 维持单写 owner；无 remote 时明确降级为本地协调。`strict` 不允许降级。跨设备交接必须显式 `handoff prepare` → `push` → `accept`；接管必须 `handoff takeover` 并记录预期旧设备和理由，不会按本机时钟自动过期 owner。

```bash
node ./bin/helix.mjs device register --name macbook
node ./bin/helix.mjs coordination status
node ./bin/helix.mjs coordination claim --task T001 --owner ZhuRong
node ./bin/helix.mjs handoff prepare --task T001 --to-device-id <uuid> --to-device-name mac-mini
node ./bin/helix.mjs handoff push --task T001
node ./bin/helix.mjs handoff accept --plan <planId> --task T001
```

新设备若尚未 `plan --from`，`handoff accept` 必须带 `--plan`。只有确认原 owner 不再写入时，才用 `handoff takeover --plan <planId> --task T001 --expected-device-id <uuid> --reason "..."` 显式接管。`gitCoordination.mode` 还有 `off` / `manual`；`strict` 不允许无 remote 降级。若远端 push 已成功但本地 checkpoint 失败，不得回滚已 push 成果，需同 run 对账或保持 `recovery_required`。

细节与不变量见 [README.md](./README.md)「运行状态与跨设备边界」和根 `AGENTS.md`。

---



## 12. 出问题了怎么办



### 一键体检

```bash
node ./bin/helix.mjs doctor
```

校验配置结构与挂载、对账已完成任务（checkpoint / 验收证明 / 账本事件必须齐全）、验证账本哈希链，并与最近一次备份交叉比对，发现整链重写。

`status` 和 `doctor` 会常驻 **`gateArming` 黄灯**：默认配置下若质量门全关、review 门没有任何独立信号，会提示「门未武装」——避免把一条不证明任何东西的全绿门流当成项目健康。可用 `config init --root --armed` 生成已武装质量门的配置。

### 账本 / 配置 / 状态校验

```bash
node ./bin/helix.mjs ledger verify           # 账本被篡改会报出来
node ./bin/helix.mjs config verify           # 质量门是否被改弱
node ./bin/helix.mjs state verify            # 关键运行态文件完整性
```



### 备份与恢复

```bash
node ./bin/helix.mjs state backup --reason before-risky-agent
node ./bin/helix.mjs state list
node ./bin/helix.mjs state restore --backup <backupId>   # 恢复前会自动再备份一次
```



### 代码被改坏了

每次 worker 执行前都有工作区快照（`git stash create`），快照哈希和恢复命令写在任务证据和账本里，可用：

```bash
git stash apply <hash>
```



### 范围越界被挡住重试

先审变更申请，accept 后再清理越界路径重试：

```bash
node ./bin/helix.mjs changes list
node ./bin/helix.mjs changes review --id CR-xxxx
node ./bin/helix.mjs changes resolve --id CR-xxxx --decision accept --evidence "同意扩大范围" --rationale "..."
# 清理仍越界的路径后
node ./bin/helix.mjs node retry --task T001
```

---



## 13. 命令速查表


| 场景           | 命令                                                                  |
| ------------ | ------------------------------------------------------------------- |
| 初始化运行时       | `node ./bin/helix.mjs init`                                         |
| 生成根配置        | `node ./bin/helix.mjs config init --root`（`--armed` 直接武装质量门） |
| 查看最终配置       | `node ./bin/helix.mjs config show`                                  |
| 安装 adapter   | `node ./bin/helix.mjs adapter install --target all --mode local`    |
| 导入计划         | `node ./bin/helix.mjs plan --from plan.json`                        |
| 确认计划         | `node ./bin/helix.mjs plan approve`                                  |
| 跑下一个任务       | `node ./bin/helix.mjs run`                                          |
| 单步跑节点        | `node ./bin/helix.mjs node execute --task T001`                     |
| 跑内置样例        | `node ./bin/helix.mjs workflow --sample`                            |
| 看状态 / 总结     | `node ./bin/helix.mjs status` / `summary`                           |
| 登记设备 / 协调   | `node ./bin/helix.mjs device register --name macbook` / `device status` / `coordination status` |
| 跨设备交接       | `node ./bin/helix.mjs handoff prepare\|push\|accept --plan <planId> --task T001` |
| 显式接管         | `node ./bin/helix.mjs handoff takeover --plan <planId> --task T001 --expected-device-id <uuid> --reason "..."` |
| 列出 / 清理并行 run | `node ./bin/helix.mjs parallel list` / `parallel cleanup --run <runId>` |
| 裁决变更申请     | `node ./bin/helix.mjs changes resolve --id CR-xxxx --decision accept --evidence "..."` |
| 仓库治理巡检     | `node ./bin/helix.mjs governance audit` |
| 收集命中规范       | `node ./bin/helix.mjs rules collect --target <path>`                |
| 匹配技能         | `node ./bin/helix.mjs skills match --text "..." --stage plan`       |
| 手动跑 hook     | `node ./bin/helix.mjs hook run --from hook.json`                    |
| 并行子 Agent    | `node ./bin/helix.mjs parallel run --max-agents 2 --command "..."`  |
| 合入 / 重试      | `node ./bin/helix.mjs parallel admit --run <runId> --task <id>` / `parallel retry --run <runId>` |
| 决策 / 时间线    | `node ./bin/helix.mjs decisions --limit 20` / `timeline` / `review suspicious` |
| 影响面 / 分区测试 | `node ./bin/helix.mjs impact src/infra/ledger.mjs` / `test --zone infra` |
| 一键体检         | `node ./bin/helix.mjs doctor`                                       |
| 账本校验         | `node ./bin/helix.mjs ledger verify`                                |
| 状态备份 / 恢复    | `node ./bin/helix.mjs state backup` / `state restore --backup <id>` |
| 启动 dashboard | `node ./bin/helix.mjs serve --host 127.0.0.1 --port 8765`           |
| 完整命令清单     | `node ./bin/helix.mjs --help --all` 或 [doc/generated/commands.md](./doc/generated/commands.md) |
| 完整测试         | `npm test`                                                          |


> 装了 adapter 后，上面大部分操作都能用 slash 命令让 AI 代跑：`/helix-config`、`/helix-doctor`、`/helix-refresh`、`/helix-status`、`/helix-plan`、`/helix-run`。

---



## 更多文档


| 文档                                                           | 说明              |
| ------------------------------------------------------------ | --------------- |
| [README.md](./README.md)                                     | 项目入口与快速上手       |
| [架构解析.md](./架构解析.md)                                     | 内部怎么运转、为什么这么设计（门控/Hook/多 Agent/追溯） |
| [CLAUDE.md](./CLAUDE.md)                                     | Agent / 开发者治理规范 |
| [doc/concept.md](./doc/concept.md)                           | 产品概念与外部参考边界     |
| [doc/project-architecture.md](./doc/project-architecture.md) | 维护者向技术地图（模块/状态文件/函数职责） |
| [doc/development-plan.md](./doc/development-plan.md)         | 路线图             |
