# WildArrange 项目规范

本文件是项目规则入口。按下表命中场景，在对应操作前读取指定正文；多项命中时合并读取，同一任务已读且未变化的内容不重复读取。代码路径相对仓库根，链接相对当前文件。

## 遇到情况先读哪里

| 情况 | 必须读取 |
| --- | --- |
| 首次进入项目，判断目标、角色或产品边界（含只读调查） | [开发与交付规范](doc/development-workflow.md)的「当前目标」「实现边界」「工程约束」。 |
| 修改任何文件、依赖、代码结构或文档 | [代码维护规范](doc/code-maintenance.md)、[目录约定](doc/project-architecture.md#目录约定)中的目标条目，以及目标路径沿途已有的 AGENTS，根、src、具体分区逐层读取。 |
| 新增、移动、删除文件，判断或改变模块职责、调用关系 | [架构文档的目录约定](doc/project-architecture.md#目录约定)中目标分区及调用双方条目；跨区依赖先按代码维护规范裁决。 |
| 执行命令，开展测试、Git 交接、验收、合并、发布、恢复或清理 | [开发与交付规范](doc/development-workflow.md)核对权限、证据与恢复边界，再查[开发命令](doc/development-commands.md)对应操作；涉及源码中的文档或测试目录时，另读对应的 doc/AGENTS.md 或 test/AGENTS.md。 |
| 安装或维护宿主 adapter、Hook、模型路由、Skill 或 Prompt Pack | 开发与交付规范，以及相关 bin/、src/ 分区、packs/wildarrange-linear/ 下的 AGENTS；先读后操作。 |
| 修改本入口、引用的规则正文或规则校验器 | 沿用用户级的明确批准要求，先展示修改内容、原因、影响及更短替代；引用正文的迁移不构成放宽约束的授权。 |

## 读取与继承

- 上述正文承接原根规则，具有同等约束力；子目录 AGENTS 只能补充或收窄，不能放松其中的不变量。普通参考文档不因被提及而自动成为新规则。
- 链接不会自动展开；收到入口不等于已读取正文。读取失败或找不到适用边界时，停止依赖该信息的操作，报告缺口，不猜测、不跳过；无关工作可继续。
- 修改项目规范仍需用户明确批准；一次具体修改的授权不得扩大为新增其他门禁、流程或永久限制的授权。
