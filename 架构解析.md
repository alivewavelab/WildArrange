# WildArrange 架构解析

> 这份文档讲「**内部怎么运转、为什么这么设计**」。
> 想知道「**怎么装、怎么用、命令怎么敲**」，看 [使用说明书.md](./使用说明书.md)。
> 想要给维护者看的英文技术地图（模块清单、状态文件、函数职责），看 [doc/project-architecture.md](./doc/project-architecture.md)。

## 目录

1. [设计哲学：一句话说清它在管什么](#1-设计哲学一句话说清它在管什么)
2. [角色分工：为什么不是一个 AI 干到底](#2-角色分工为什么不是一个-ai-干到底)
3. [门控模型：完成的六道门](#3-门控模型完成的六道门)
4. [会话级注入机制（Hook）](#4-会话级注入机制hook)
5. [记忆与追溯：改坏了怎么救](#5-记忆与追溯改坏了怎么救)
6. [多 Agent 合入原理](#6-多-agent-合入原理)
7. [路由与档案路由员](#7-路由与档案路由员)
8. [安全边界](#8-安全边界)

---

## 1. 设计哲学：一句话说清它在管什么

它要解决的不是"让 AI 会写代码"，而是"**让一群会写代码的 AI 不失控**"。核心就五条运行秩序：

```text
计划与执行分离 → worker 不自证完成 → 独立验证 → 失败返工 → 证据入账
```

- **计划与执行分离**：先把需求拆成结构化计划（谁改哪、验收标准、依赖关系），再动手。不允许边想边改。
- **worker 不自证完成**：干活的 AI 说"我做完了"只是一个**声称**，不作数。必须由独立的验证/复核角色核对。
- **独立验证**：验收命令、范围守卫、复核门是分开跑的程序化关卡，不受干活 AI 的话术影响。
- **失败返工**：任一关没过，任务被打回，失败原因入账，而不是硬着头皮往下走。
- **证据入账**：每一步都往带哈希链的账本里追加事件、写快照。任何一步都能被追溯、被恢复。

所有运行时状态都写在项目根的 `.helix/` 目录里，是**产品中立的本地文件协议**——不绑定任何特定编辑器。

---

## 2. 角色分工：为什么不是一个 AI 干到底

如果同一个 AI 既写代码又给自己打分，"我做完了"就没人能证伪。所以流程保留 5 个长期 Agent，并把窄职责改成按需 Skill：**谁编排、谁干活、谁独立验收仍然分离**。

默认角色（完整表和"在哪配置"见 [使用说明书 §7.1/§7.2](./使用说明书.md#7-配置文件-helixconfigjson含-agent-角色与在哪配置)）：

| 层 | 角色 | 职责 |
|---|---|---|
| 编排 | `Jiuwei` | 总指挥兼线性执行官：派发任务并推进交付闭环 |
| 计划 | `DiJiang` | 澄清目标、拆计划、定义范围与验收 |
| 执行 | `ZhuRong` | 真正改代码（worker） |
| 验收 | `BaiZe` | 唯一独立复核者；按 Skill 切换目标、风险与怀疑式视角 |
| 治理 | `LuWu` | 只读检查目录、AGENTS、README、命名、归属和注释 |

**关键点：范围守卫、验收证明、checkpoint 不是 Agent，而是"系统 gate"（规则程序）。** 它们不讲道理、不被话术说服，只按硬规则判定，用来防止任何 Agent（哪怕是总指挥）绕过关卡。这是"AI 之上还有一层不可协商的秩序"的落点。

---

## 3. 门控模型：完成的六道门

一个任务要从"开始"走到"真正完成"，必须**依次全过**六道门，缺一不可：

| # | 门 | 谁把关 | 判定 |
|---|---|---|---|
| 1 | worker 退出码 | 系统 | `worker_command` 必须 exit 0 |
| 2 | 独立验证 | `BaiZe` | `verify_commands` 存在且全部通过 |
| 3 | 验收标准 | 系统 | `successCriteria` 通过（有独立证据，不是照抄 verifier） |
| 4 | 范围守卫 | 系统 | 改动路径全部落在 `writable_paths` 白名单内 |
| 5 | 复核门 | `BaiZe` + 按需 Review Skills | 主 lane PASS，安全无 high/critical |
| 6 | 验收证明 | 系统 | 再把 1~5 的证据链逐项核验，并**拒绝 no-op 任务** |

- **`inconclusive`（证据不足）不算通过。** 拿不到 Git 改动信息、证据缺失，都只能判 inconclusive，任务不能完成。
- **no-op 任务硬拦：** 空转 worker + 空转 verifier + 没有 `writable_paths` 的任务会被验收证明直接拒，防止"什么都没干却标完成"。
- 六道门全过 → 任务 `completed`、写 checkpoint、追加经验、写完成小结（人话解释见 [使用说明书 §8 步骤 6](./使用说明书.md#8-主线工作流逐步拆解含每步负责-agent--主动被动--人话)）。

复核门是宿主中立的：从 CLI 跑，可包含确定性 lane、配置的 `review_commands` / `standards_commands`、LSP/类型检查、AST 结构检查、hashline anchor、注释检查，以及可选的 LLM 复核。

---

## 4. 会话级注入机制（Hook）

装了 adapter 后，宿主编辑器会在**关键时机**自动调用 `hook run`；运行时按当前事件挑选注入点，把该挂的规则/技能/状态拼成上下文块返回给模型。开发者不用手动触发——这属于**被动注入**。

六个注入点：

| 触发事件 | 注入点 | 做了什么 |
|---|---|---|
| `SessionStart` | `session_start` | 恢复上一次进度、扫规则、构建 Agent 上下文、触发档案路由、写 digest |
| `UserPromptSubmit` | `user_prompt_submit` | 对请求做路由决策 + 补规则 + 触发档案路由 |
| `PreToolUse` | `pre_tool_use` | 工具执行**前**做范围预检；计划外写入返回 `permissionDecision=deny` 拦下 |
| `PostToolUse` | `post_tool_use` | 工具执行**后**按目标文件刷新动态规则 + 工具结果门 + 范围检查 |
| `PostCompact` | `post_compact` | 上下文被压缩后，恢复工作状态和规则 |
| `Stop` | `stop` | 会话停止前生成续跑指令，下次自动接上 |

### 动态技能挂载（按需，不是全塞）

技能不是每次会话都全量塞进去，而是**按请求文本 + 阶段按需挂载**（`skillMatcher.dynamicInjection`，默认开）：

- 注入点上配置的技能列表是**上限**；动态匹配只做减法，绝不会挂列表外的技能全文。
- `alwaysMount`（默认 `wildarrange-injection-runtime`）永远挂；其余技能只有在请求相关信号上打分 > 0 才挂。**仅凭 Agent 身份加成不算命中**（否则会退化成静态全挂）。
- `maxSkills`（默认 4）限制动态挂载数量。
- 没被挂的技能降级为"按需引用"（给名字 + 加载命令），模型需要时再显式拉取。
- 拿不到请求文本时（如 `pre_tool_use`），回退静态挂载并在 `skillSelection` 里说明原因。

### 上下文预算（防止把窗口撑爆）

注入内容分级管理，各有字符预算，超了必须显式标 `truncated`（不允许静默截断）：

- Prompt 变体：短而稳，默认 12,000 字符。
- Markdown 挂载（规则/快照/实时状态）：默认 12,000 字符，红绿灯类 hook 更省。
- 技能挂载（工作流手册）：默认 80,000 字符，execute/review 类 hook 更宽。

设计隐喻：**系统提示词像宪法，技能像任务手册，引用像档案馆，hook 像红绿灯。**

### 人工决策通道（通用推送）

系统不绑飞书/邮件等外部推送。所有"待你拍板"的事项（计划待确认、改动越界待审、任务失败、子 Agent 待验收）由 `attentionReport` 汇总，再经 hook 在 `SessionStart` / `UserPromptSubmit` / `PostCompact` / `Stop` 时机**注入宿主 AI 上下文，要求它主动向你复述、给选项、等你答复**。你在对话里回答即可。

---

## 5. 记忆与追溯：改坏了怎么救

这套系统的"兜底"由几层证据构成：

- **带哈希链的账本 `.helix/ledger.jsonl`**：每步追加事件，前后事件用哈希串成链。普通改一行、断链、或在链启动后插入未哈希的行，`ledger verify` 都能报出来——**篡改藏不住**。`doctor` 只把通过哈希校验的事件当作完成证据。
- **执行前工作区快照**：每次 worker 动手前，先用 `git stash create` 拍一张快照，哈希写进任务证据和账本。改坏了用 `git stash apply <hash>` 还原。
- **恢复快照 `.helix/snapshots/context.md`**：记录进度，供续跑恢复。
- **记忆 digest**：任务/会话/压缩点的结构化小结，跨会话恢复时 AI 一看就知道之前干到哪。
- **经验库（wisdom）**：任务完成时追加"这次学到啥"，下次遇到同类活儿能召回、少踩坑。
- **备份与一键恢复**：`state backup` 给账本/任务/配置基线等打时间点备份，`state restore` 恢复前会自动再备份一次。
- **一致性体检 `doctor`**：核对已完成任务的 checkpoint / 验收证明 / 账本事件是否齐全，校验账本哈希链，并与最近备份交叉比对，发现整链重写。

---

## 6. 多 Agent 合入原理

第一版多 Agent 刻意做窄：命令型子 Agent 可以在**隔离运行目录**或 **Git worktree** 里并行干活，但**同样不能自证完成**。

合入闭环：

1. 子 Agent 把结构化成果写进 `agent-result.json`（`files[]` 或 worktree `patch`）。
2. `parallel admit` 先检查 `writable_paths`，越界直接拒。
3. 通过后把文件/patch 应用到主工作区，再依次跑 **验证 → 范围守卫 → 复核门 → 验收证明 → checkpoint**，全过才能 `completed`。
4. 合入失败会**回滚**主工作区改动，子 Agent 结果保留待修。
5. 成功的子 Agent 结果先进 `awaiting_user_acceptance`，用户显式验收后才 `released`。

要点：隔离让并行不互相污染，但"最终能否进主线"仍由主线的全套 gate 说了算——子 Agent 没有捷径。

---

## 7. 路由与档案路由员

路由是混合模型，兼顾"可解释的确定性"和"语义第二意见"：

1. `routes.json` 跑确定性热路径（关键词 → 类别/复杂度/意图）。
2. `routeGovernance.semanticShadow` 可让 `CangJie` 出语义第二意见；确定性结果仍可见，但低置信或冲突的 execute 路由可被降级为 plan/ask。
3. `ArchivistRouter`（档案路由员）在有外部模型 key 时用配置的 `CangJie`/`deepseek-v4-flash`，无 key 时**回退确定性路由，不阻断主线**。
4. hook 在 `SessionStart`/`UserPromptSubmit`/`PostCompact` 触发它，且**非阻断**：失败只变成告警事实，不会拦下你的请求。
5. 它只读**有界的路由包和结构化记忆**（结论级捕获：意图、可见结论、证据、进度、决策、踩坑、开放问题），**默认剥离代码块、diff、原始命令输出**——不摄入无限原始对话。
6. 关键词建议先写成待审工单；accept 后更新 `.helix/routing/routes-overrides.json`，**不改动安装的 prompt 包源文件**。review、Git、权限、安全、删除、发布、范围变更等高风险区改路由必须带证据和理由。

---

## 8. 安全边界

- **命令安全预检**：worker/verifier/review 命令执行前过高危正则。删 `.git`/`.helix`、递归删 `src`/`test`/`doc`、`git reset --hard`、`git clean -fd`、`sudo`、`curl | sh` 等直接拦下（退出码 126）。内置正则是**不可削弱的地板**，`commandSafety.extraPatterns` 只能在其上**追加**项目专属拦截。
- **配置基线**：`config baseline` 记录配置指纹，`config verify` 能发现质量门被改弱、配置被增删改。
- **Dashboard 本地优先**：默认只绑 `127.0.0.1`；每个 `POST` 端点（可能触发 worker 命令）即使在本机也要 token，并校验 Host / Origin，降低 DNS 重绑定和浏览器跨站触发风险。
- **商业边界**：core 必须是原创代码；外部工作流项目只能作为概念参考，商业发布包不得夹带受限第三方源码、prompt 原文或近似改写文本。

---

## 更多文档

| 文档 | 说明 |
|---|---|
| [使用说明书.md](./使用说明书.md) | 怎么装、怎么用、命令速查、两个端到端范例 |
| [doc/project-architecture.md](./doc/project-architecture.md) | 英文技术地图：模块清单、状态文件、函数职责 |
| [CLAUDE.md](./CLAUDE.md) | Agent / 开发者治理规范 |
| [doc/concept.md](./doc/concept.md) | 产品概念与外部参考边界 |
