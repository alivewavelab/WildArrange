# WildArrange 架构解析

> 这份文档讲「**内部怎么运转、为什么这么设计**」。
> 想知道「**怎么装、怎么用、命令怎么敲**」，看 [使用说明书.md](./使用说明书.md)。
> 想要给维护者看的技术地图（模块清单、状态文件、函数职责），看 [doc/project-architecture.md](./doc/project-architecture.md)。

## 目录

1. [设计哲学：一句话说清它在管什么](#1-设计哲学一句话说清它在管什么)
2. [角色分工：为什么不是一个 AI 干到底](#2-角色分工为什么不是一个-ai-干到底)
3. [门控模型：完成的六道门](#3-门控模型完成的六道门)
4. [会话级注入机制（Hook）](#4-会话级注入机制hook)
5. [记忆与追溯：改坏了怎么救](#5-记忆与追溯改坏了怎么救)
6. [多 Agent 合入原理](#6-多-agent-合入原理)
7. [跨设备所有权与 admission 集成](#7-跨设备所有权与-admission-集成)
8. [路由与档案路由员](#8-路由与档案路由员)
9. [安全边界](#9-安全边界)

---

## 1. 设计哲学：一句话说清它在管什么

它要解决的不是"让 AI 会写代码"，而是"**让一群会写代码的 AI 不失控**"。核心就五条运行秩序：

```text
计划与执行分离 → worker 不自证完成 → 独立验证 → 失败返工 → 证据入账
```

- **计划与执行分离**：先把需求拆成结构化计划（谁改哪、验收标准、依赖关系），再动手。不允许边想边改。
- **worker 不自证完成**：干活的 AI 说"我做完了"只是一个**声称**，不作数。必须由独立的验证/复核角色核对。
- **独立验证**：验收命令、范围守卫、复核门是分开跑的程序化关卡，不受干活 AI 的话术影响。
- **失败返工**：任一关没过，任务被打回，失败原因入账，而不是硬着头皮往下走。
- **证据入账**：每一步都往带哈希链的账本里追加事件、写快照。任何一步都能被追溯、被恢复。

所有运行时状态都写在项目根的 `.wildarrange/` 目录里，是**产品中立的本地文件协议**——不绑定任何特定编辑器。

---

## 2. 角色分工：为什么不是一个 AI 干到底

如果同一个 AI 既写代码又给自己打分，"我做完了"就没人能证伪。所以流程保留 5 个长期 Agent，并把窄职责改成按需 Skill：**谁编排、谁干活、谁独立验收仍然分离**。

默认角色（完整表和"在哪配置"见 [使用说明书 §7.1/§7.2](./使用说明书.md#7-配置文件-wildarrangeconfigjson含-agent-角色与在哪配置)）：

| 层 | 角色 | 职责 |
|---|---|---|
| 编排 | `Jiuwei` | 总指挥兼线性执行官：派发任务并推进交付闭环 |
| 计划 | `DiJiang` | 澄清目标、拆计划、定义范围与验收 |
| 执行 | `ZhuRong` | 真正改代码（worker） |
| 验收 | `BaiZe` | 唯一独立复核者；按 Skill 切换目标、风险与怀疑式视角 |
| 治理 | `LuWu` | 只读检查目录、AGENTS、README、命名、归属和注释 |

**Router 与 CangJie 不是第六、第七个长期 Agent。** Router 是确定性系统节点，负责 intent / category / 主 Agent / Skill 路由，不占 `agent-registry` 白名单。CangJie 是可选内部 profile，只给 semantic shadow 与 ArchivistRouter 做第二意见，不能当 command worker。

**关键点：范围守卫、验收证明、checkpoint 不是 Agent，而是"系统 gate"（规则程序）。** 它们不讲道理、不被话术说服，只按硬规则判定，用来防止任何 Agent（哪怕是总指挥）绕过关卡。这是"AI 之上还有一层不可协商的秩序"的落点。

---

## 3. 门控模型：完成的六道门

一个任务要从"开始"走到"真正完成"，必须过完下列六道门，缺一不可。**执行顺序**是 worker（pipeline 外）→ `verify → scope → review`（即使前面 FAIL 也全跑以留证据）→ 一并判定 worker / criteria / 三门 → 验收证明 →（Git guarded/strict 下先集成 commit+push）→ checkpoint。`successCriteria` 不是插在 verify 与 scope 中间的独立步骤。

| # | 门 | 谁把关 | 判定 |
|---|---|---|---|
| 1 | worker 退出码 | 系统 | `worker_command` 必须 exit 0（在 delivery pipeline 之前执行） |
| 2 | 独立验证 | 系统 verifier | `verify_commands` 存在且全部通过 |
| 3 | 范围守卫 | 系统 | 改动路径全部落在 `writable_paths` 内；`inconclusive` 不算通过 |
| 4 | 复核门 | `BaiZe` + 按需 Review Skills / 质量门 | 主 lane PASS，安全无 high/critical |
| 5 | 验收标准 | 系统 | `successCriteria` 通过（独立证据，不是照抄 verifier）；与 1~4 在 pipeline 末尾一并判定 |
| 6 | 验收证明 | 系统 | 再逐项核验 1~5 的证据链，并拒绝 no-op / trivial verify / 同义反复 review |

- **`inconclusive`（证据不足）不算通过。** 拿不到 Git 改动信息、证据缺失，都只能判 inconclusive，任务不能完成。
- **no-op 任务硬拦：** 空转 worker + 空转 verifier + 没有 `writable_paths` 的任务会被验收证明直接拒。
- **trivial verify 硬拦：** 全部 `verify_commands` 都是空转（如 `true`）时，以 `verify_not_trivial` 拒绝。
- **同义反复 review 硬拦：** 没有任何独立复核信号（`review_commands` / `standards_commands` / `review.llm` / 已启用质量门）时，以 `review_not_tautological` 拒绝。
- 六道门全过 → 任务 `completed`、写 checkpoint、追加经验、写完成小结（人话解释见 [使用说明书 §8 步骤 6](./使用说明书.md#8-主线工作流逐步拆解含每步负责-agent--主动被动--人话)）。

复核门是宿主中立的：从 CLI 跑，可包含确定性 lane、配置的 `review_commands` / `standards_commands`、LSP/类型检查、AST 结构检查、hashline anchor、注释检查，以及可选的 LLM 复核。

---

## 4. 会话级注入机制（Hook）

装了 adapter 后，宿主编辑器会在**关键时机**自动调用 `hook run`；运行时按当前事件挑选注入点，把该挂的规则/技能/状态拼成上下文块返回给模型。开发者不用手动触发——这属于**被动注入**。

宿主生命周期会映射到下面六个 **Hook 注入点**；配置里还有编排注入点（`before_execute` / `before_review` / `before_checkpoint` / `repository_governance`），完整列表见 `config show` 的 `injectionPoints`。

| 触发事件 | 注入点 | 做了什么 |
|---|---|---|
| `SessionStart` | `session_start` | 恢复上一次进度、扫规则、构建 Agent 上下文、触发档案路由、写 digest |
| `UserPromptSubmit` | `user_prompt_submit` | 对请求做路由决策 + 补规则 + 触发档案路由 |
| `PreToolUse` | `pre_tool_use` | 工具执行**前**做范围预检；计划外写入返回 `permissionDecision=deny` 拦下 |
| `PostToolUse` | `post_tool_use` | 工具执行**后**按目标文件刷新动态规则 + 工具结果门 + 范围检查 |
| `PostCompact` | `post_compact` | 上下文被压缩后，恢复工作状态和规则 |
| `Stop` | `stop` | 会话停止前生成续跑指令，下次自动接上 |

### 动态技能挂载（按需，不是全塞）

技能不是每次会话都全量塞进去，而是**按请求文本 + 阶段按需挂载**（`skillMatcher.dynamicInjection`，默认开）：

- 动态 matcher 只在注入点与 Agent 的显式技能集合内做减法；`task.skills` 是受安全加载与数量预算约束的额外显式来源，只由执行前公开宿主入口自动消费。
- `alwaysMount`（默认 `wildarrange-injection-runtime`）永远挂；其余技能只有在请求相关信号上打分 > 0 才挂。**仅凭 Agent 身份加成不算命中**（否则会退化成静态全挂）。
- `maxSkills`（默认 4）限制动态挂载数量。
- 没被挂的技能降级为"按需引用"（给名字 + 加载命令），模型需要时再显式拉取。
- 拿不到请求文本时（如 `pre_tool_use`），回退静态挂载并在 `skillSelection` 里说明原因。

### 上下文预算（防止把窗口撑爆）

注入内容分级管理，各有字符预算，超了必须显式标 `truncated`（不允许静默截断）：

- Agent 身份 Prompt：短而稳，默认 12,000 字符；独立 Prompt 变体已退役。
- Markdown 挂载（规则/快照/实时状态）：默认 12,000 字符，红绿灯类 hook 更省。
- 技能挂载（工作流手册）：默认 80,000 字符，execute/review 类 hook 更宽。

设计隐喻：**系统提示词像宪法，技能像任务手册，引用像档案馆，hook 像红绿灯。**

### 人工决策通道（通用推送）

系统不绑飞书/邮件等外部推送。所有"待你拍板"的事项（计划待确认、改动越界待审、任务失败、子 Agent 待验收）由 `attentionReport` 汇总，再经 hook 在 `SessionStart` / `UserPromptSubmit` / `PostCompact` / `Stop` 时机**注入宿主 AI 上下文，要求它主动向你复述、给选项、等你答复**。你在对话里回答即可。

---

## 5. 记忆与追溯：改坏了怎么救

这套系统的"兜底"由几层证据构成：

- **带哈希链的账本 `.wildarrange/ledger.jsonl`**：每步追加事件，前后事件用哈希串成链。普通改一行、断链、或在链启动后插入未哈希的行，`ledger verify` 都能报出来——**篡改藏不住**。`doctor` 只把通过哈希校验的事件当作完成证据。
- **执行前工作区快照**：每次 worker 动手前，先用 `git stash create` 拍一张快照，哈希写进任务证据和账本。改坏了用 `git stash apply <hash>` 还原。
- **恢复快照 `.wildarrange/snapshots/context.md`**：记录进度，供续跑恢复。
- **记忆 digest**：任务/会话/压缩点的结构化小结，跨会话恢复时 AI 一看就知道之前干到哪。
- **经验库（wisdom）**：任务完成时追加"这次学到啥"，下次遇到同类活儿能召回、少踩坑。
- **备份与一键恢复**：`state backup` 给账本/任务/配置基线等打时间点备份；归档会把精确删除集追加为 recovery package，`state restore` 可连同 Plan、证明、DoneClaim 和 artifact 一起恢复，且恢复前会自动再备份一次。
- **一致性体检 `doctor`**：核对已完成任务的 checkpoint / 验收证明 / 账本事件是否齐全，校验账本哈希链，并与最近备份交叉比对，发现整链重写。

---

## 6. 多 Agent 合入原理

第一版多 Agent 刻意做窄：命令型子 Agent 可以在**隔离运行目录**或 **Git worktree** 里并行干活，但**同样不能自证完成**。`--agent` 只能是可写身份（`Jiuwei` / `ZhuRong`）；`DiJiang`、`BaiZe`、`LuWu` 不得进入 command worker。

合入闭环：

1. 子 Agent 把结构化成果写进 `agent-result.json`（`files[]` 或 worktree `patch`）。
2. `parallel admit` 先检查 `writable_paths`，越界直接拒。
3. 通过后把文件/patch 应用到主工作区，再依次跑 **验证 → 范围守卫 → 复核门 → 验收证明 → checkpoint**，全过才能 `completed`。
4. 合入失败会**回滚**主工作区改动，子 Agent 结果保留待修。回滚失败保留 `verifying` claim 与 rollback plan，返回 `recovery_required`，必须由原 run 恢复。
5. 子 Agent run 成功后默认进入 `awaiting_user_acceptance`；`parallel admit` 走完全部 gate + checkpoint 后 lifecycle 变为 `released`。`parallel close --reason user_accepted` 把保留结果标成 `closed`，不是 admission 释放 ownership 的前置条件。
6. Git `guarded`/`strict` 下 admission 还要过远端 SHA 基线与无归属改动检查：失败返回 `revalidation_required`；gate 全过后必须先普通 push 集成 commit，才允许 checkpoint。
7. **回滚失败**或**集成 push 已成功但 checkpoint/对账失败**：返回 `recovery_required`，保留 claim，禁止换 run 或回滚远端。**push 尚未发生时的 checkpoint 失败**：工作区已回滚、claim 释放，任务回 `pending`，可重跑 admission。

要点：隔离让并行不互相污染，但"最终能否进主线"仍由主线的全套 gate 说了算——子 Agent 没有捷径。

---

## 7. 跨设备所有权与 admission 集成

`gitCoordination.mode` 有 `off` / `manual` / `guarded`（默认）/ `strict`。`guarded` 有 remote 时用任务分支 claim 维持单写 owner，可写并行 Agent 默认 worktree；无 remote 时明确降级为本地协调。`strict` 不允许任何降级。`off` / `manual` 不自动抢远端 owner。

- 同一任务禁止双写；handoff 必须绑定已 push commit，push 前复核 prepare 树指纹。
- takeover 必须显式记录预期旧设备和理由，不会按本机时间自动过期 owner。
- 任意设备可执行 admission，但开始时必须绑定远端集成分支 SHA，工作区必须包含该基线，且不得夹带本 run / handoff 清单之外的无归属改动。
- gate 期间 owner/SHA 变化或基线落后必须安全回滚并返回 `revalidation_required`，不得写 acceptance proof / checkpoint。
- 全部 gate 与验收证明通过后，必须生成以该 SHA 为父提交的集成 commit 并普通 push；push 已成功后任何本地/所有权/远端历史故障都不得回滚原 run，只能对账或保持 `recovery_required`。

命令：`device register`、`coordination status|claim`、`handoff prepare|push|accept|takeover`。不变量见根 `AGENTS.md`。

---

## 8. 路由与档案路由员

路由是混合模型，兼顾"可解释的确定性"和"语义第二意见"：

1. `routes.json` 跑确定性热路径（关键词 → 类别/复杂度/意图）。
2. `routeGovernance.semanticShadow` 可让 `CangJie` 出语义第二意见；确定性结果仍可见，但低置信或冲突的 execute 路由可被降级为 plan/ask。
3. `ArchivistRouter`（档案路由员）在有外部模型 key 时用配置的 `CangJie`/`deepseek-v4-flash`，无 key 时**回退确定性路由，不阻断主线**。
4. hook 在 `SessionStart`/`UserPromptSubmit`/`PostCompact` 触发它，且**非阻断**：失败只变成告警事实，不会拦下你的请求。另按 stage 感知轮次（ideate/plan ~5、常规 ~10、execute/verify/review ~15，上限 20）和 Git HEAD 变化调度。
5. 它只读**有界的路由包和结构化记忆**（结论级捕获：意图、可见结论、证据、进度、决策、踩坑、开放问题），**默认剥离代码块、diff、原始命令输出**——不摄入无限原始对话。
6. 关键词建议先写成待审工单；accept 后更新 `.wildarrange/routing/routes-overrides.json`，**不改动安装的 prompt 包源文件**。review、Git、权限、安全、删除、发布、范围变更等高风险区改路由必须带证据和理由。

---

## 9. 安全边界

- **命令安全预检**：worker/verifier/review 命令执行前过高危正则。删 `.git`/`.wildarrange`、递归删 `src`/`test`/`doc`、`git reset --hard`、`git clean -fd`、`sudo`、`curl | sh` 等直接拦下（退出码 126）。内置正则是**不可削弱的地板**，`commandSafety.extraPatterns` 只能在其上**追加**项目专属拦截。
- **配置基线**：`config baseline` 记录配置指纹，`config verify` 能发现质量门被改弱、配置被增删改。
- **Dashboard 本地优先**：默认只绑 `127.0.0.1`；loopback 下 `GET /api/state` 可免 token 只读；**所有 POST 写操作**（含本机）必须带 token，并校验 Host / Origin，降低 DNS 重绑定和浏览器跨站触发风险。
- **宿主 Hook 边界**：Cursor `preToolUse` 等工作区受信时可 fail-closed；Kimi plugin Hook 在崩溃/超时时 fail-open。Hook 只能做早期拦截，**不能**替代 verifier / scope / review / acceptance-proof / checkpoint 完成链。
- **商业边界**：core 必须是原创代码；外部工作流项目只能作为概念参考，商业发布包不得夹带受限第三方源码、prompt 原文或近似改写文本。

---

## 更多文档

| 文档 | 说明 |
|---|---|
| [使用说明书.md](./使用说明书.md) | 怎么装、怎么用、命令速查、两个端到端范例 |
| [doc/project-architecture.md](./doc/project-architecture.md) | 维护者技术地图：模块清单、状态文件、函数职责 |
| [CLAUDE.md](./CLAUDE.md) | Agent / 开发者治理规范 |
| [doc/concept.md](./doc/concept.md) | 产品概念与外部参考边界 |
