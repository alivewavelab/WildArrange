---
description: <一句话说明这条规范管什么，例如：后端接口与错误处理规范>
globs: [src/backend/**, server/**, api/**, "**/*.go", "**/*.py"]
---

<!--
  这是【后端规范空架子】。只有当本次任务改动的文件命中上面的 globs 时才会被注入。
  路径没有前后端标注时，靠文件后缀区分（如 **/*.go、**/*.py）。
  填完把这段说明和所有 <...> / [ ] 占位替换成你的真实规范。
-->

# 后端编码规范

## 分层与边界
- [ ] <例如：Use Case 不直接依赖具体存储；外部调用走 Adapter>

## 接口契约
- [ ] <例如：对外接口变更必须先更新契约文档，再改实现>

## 错误处理与日志
- [ ] <例如：不吞异常；错误必须带可定位上下文；禁止打印敏感信息>

## 数据与安全
- [ ] <例如：所有外部输入必须校验；查询参数化，禁止拼接 SQL>

## 验收（改完后端必须做的事）
- [ ] <例如：跑接口测试；给出关键路径的验证证据>
