---
description: <一句话说明这条规范管什么，会显示在注入摘要里，例如：前端组件与状态规范>
globs: [src/frontend/**, apps/web/**, "**/*.tsx", "**/*.vue"]
---

<!--
  这是【前端规范空架子】。只有当本次任务改动的文件命中上面的 globs 时才会被注入。
  globs 两种写法都支持：
    - 按目录/路径：src/frontend/**、apps/web/**
    - 按文件后缀：**/*.tsx、**/*.vue
  填完把这段说明和所有 <...> / [ ] 占位替换成你的真实规范。
-->

# 前端编码规范

## 组件
- [ ] <组件拆分/命名约束，例如：单组件不超过 200 行，Props 必须显式类型>

## 状态管理
- [ ] <状态方案约束，例如：全局状态统一走 X，禁止在组件里直接改全局单例>

## 样式
- [ ] <样式约束，例如：只用设计 token，不写魔法颜色值>

## 验收（改完前端必须做的事）
- [ ] <例如：跑 typecheck 与单测；UI 改动附浏览器验收截图作为证据>
