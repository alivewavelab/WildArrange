---
description: <一句话说明这条规范管什么，例如：数据库 schema 与迁移规范>
globs: [migrations/**, db/**, "**/schema.prisma", "**/*.sql"]
---

<!--
  这是【数据库/迁移规范空架子】。只有当本次任务改动命中上面的 globs 时才会被注入。
  填完把这段说明和所有 <...> / [ ] 占位替换成你的真实规范。
-->

# 数据库 / 迁移规范

## Schema 变更
- [ ] <例如：字段变更必须向后兼容，先加后删，禁止直接改列语义>

## 迁移
- [ ] <例如：每次迁移必须可回滚；破坏性迁移需人工二次确认>

## 索引与性能
- [ ] <例如：新增高频查询必须评估索引；避免全表扫描>

## 验收（改完数据库必须做的事）
- [ ] <例如：在影子库跑迁移并给出 up/down 验证证据>
